{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build solver",
      "type": "shell",
      "command": "make",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"]
    },
    {
      "label": "Run solver",
      "type": "shell",
      "command": "make run",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "dependsOn": "Build solver",
      "problemMatcher": []
    }
  ]
}
